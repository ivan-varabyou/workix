# Правила организации MD файлов в проекте Workix

**Версия**: 1.0
**Дата**: 2025-01-27

## 🎯 Цель

Предотвратить накопление MD файлов в корне проекта и обеспечить чистую структуру для всех микросервисов и фронтенда.

## 📁 Структура хранения

### 1. Постоянная документация проекта
**Путь**: `.docs/`

**Содержит**:
- Архитектурная документация (`.docs/architecture/`)
- Руководства (`.docs/guides/`)
- API документация (`.docs/api/`)

**Правило**: Только файлы, которые нужны долгосрочно.

### 2. Спецификации приложений
**Путь**: `apps/{app-name}/.specs/`

**Содержит**:
- Спецификации функциональности
- Планы реализации
- Чек-листы качества
- Анализ согласованности

**Правило**: Все спецификации конкретного приложения хранятся в его `.specs/` папке.

### 3. Системные спецификации
**Путь**: `.specify/specs-optimized/`

**Содержит**:
- Общие спецификации проекта
- Правила разработки
- Git workflow
- Архитектурные решения

**Правило**: Только системные спецификации, общие для всего проекта.

### 4. Конфигурация Cursor
**Путь**: `.cursor/`

**Содержит**:
- Настройки Cursor
- MCP конфигурации
- Команды и скрипты

**Правило**: Только конфигурационные файлы для Cursor.

### 5. Архивные документы
**Путь**: `.docs/archive/`

**Содержит**:
- Завершенные миграции
- Завершенные рефакторинги
- Старые анализы
- Устаревшие планы

**Правило**: Файлы старше 30 дней или завершенные задачи автоматически архивируются.

## 🔍 Классификация файлов

### Постоянные файлы (остаются)
- `README.md` (в корне и в приложениях)
- `ARCHITECTURE_*.md` → `.docs/architecture/`
- `GUIDE_*.md` → `.docs/guides/`
- `API_*.md` → `.docs/api/`

### Файлы приложений (в `.specs/`)
**ВСЕ спецификации приложения хранятся в `apps/{app}/.specs/`!**

- `{APP}_PLAN.md` или `spec.md` → `apps/{app}/.specs/`
- `{APP}_SECURITY.md` → `apps/{app}/.specs/`
- `IMPLEMENTATION_PLAN.md` или `plan.md` → `apps/{app}/.specs/`
- `tasks.md` → `apps/{app}/.specs/`
- `ANALYSIS_REPORT.md` или `analysis.md` → `apps/{app}/.specs/`
- `SPECKIT_WORKFLOW.md` → `apps/{app}/.specs/`
- `COMMANDS_ORDER.md` → `apps/{app}/.specs/`
- `checklists/*.md` → `apps/{app}/.specs/checklists/`

**Правило**: Все документы, созданные командами `/speckit.*`, автоматически сохраняются в `.specs/` соответствующего приложения!

### Архивные файлы (в `.docs/archive/`)
- `MIGRATION_*.md` → `.docs/archive/migrations/`
- `REFACTORING_*.md` → `.docs/archive/refactoring/`
- `*_ANALYSIS.md` → `.docs/archive/analysis/`
- `PLAN_*.md` (старые) → `.docs/archive/plans/`
- `LIBS_STRUCTURE_*.md` → `.docs/archive/structure/`

### Временные файлы (удаляются)
- `TEST_*.md`, `*_TEST.md` → удаляются после завершения
- `REPORT_*.md`, `*_REPORT.md` → удаляются через 7 дней
- `TEMP_*.md`, `*_TEMP.md` → удаляются сразу
- `CHECK_*.md`, `*_CHECK.md` → удаляются после проверки

## 🤖 Автоматизация

### Скрипт организации
```bash
# Организовать все MD файлы
make docs-organize
```

**Действия**:
1. Сканирует корень проекта на MD файлы
2. Классифицирует по правилам
3. Перемещает в соответствующие папки
4. Обновляет ссылки в README файлах

### Скрипт архивации
```bash
# Архивировать старые файлы (>30 дней)
make docs-archive
```

**Действия**:
1. Находит файлы старше 30 дней
2. Перемещает в `.docs/archive/`
3. Создает индекс архива

### Скрипт очистки
```bash
# Очистить временные файлы
make docs-cleanup
```

**Действия**:
1. Удаляет временные файлы
2. Очищает старые отчеты (>7 дней)
3. Очищает пустые папки

## 📋 Правила для AI агента

### При создании MD файла:
1. **Спецификация приложения** → `apps/{app}/.specs/`
2. **Системная спецификация** → `.specify/specs-optimized/`
3. **Архитектурная документация** → `.docs/architecture/`
4. **Временный отчет** → `.docs/reports/` (автоматически архивируется)

### После завершения операции:
1. Запустить `make docs-organize` для организации новых файлов
2. Запустить `make docs-archive` для архивации старых
3. Запустить `make docs-cleanup` для очистки временных

### Исключения:
- `README.md` в корне проекта - всегда остается
- `TASKS.md` в корне проекта - всегда остается
- `SPECKIT_COMMANDS.md` в корне проекта - всегда остается

## 🚫 Запрещено

- ❌ Создавать MD файлы в корне проекта (кроме README.md, TASKS.md, SPECKIT_COMMANDS.md)
- ❌ Хранить временные файлы дольше 7 дней
- ❌ Дублировать документацию в разных местах
- ❌ Создавать файлы без четкой классификации

## ✅ Рекомендуется

- ✅ Использовать `.specs/` для спецификаций приложений
- ✅ Использовать `.docs/` для постоянной документации
- ✅ Регулярно запускать `make docs-organize`
- ✅ Архивировать завершенные задачи
- ✅ Удалять временные файлы после использования

## 📚 См. также

- [Структура .specs/](../.specs-template/README.md)
- [Документация проекта](../.docs/README.md)
- [Системные спецификации](./specs-optimized/index.md)
