# 📊 Анализ соresponseствия эндпоинтов спецификации

**Date:** 2025-11-16
**Сервис:** api-monolith
**Спецификация:** `.specify/specs/000-project/API_GATEWAY_ENDPOINTS.md`

---

## 🔍 Разделение responseственности: Auth Service vs Monolith

### ✅ Auth Service (api-auth, порт 7200)

**Должно быть в Auth Service:**
- ✅ Регистрация и вход (`/auth/register`, `/auth/login`)
- ✅ Управление токенами (`/auth/verify`, `/auth/refresh`)
- ✅ Получение текущего пользователя (`/auth/me`) - **ВАЖНО: это в auth!**
- ✅ OAuth2 (`/auth/oauth2/*`)
- ✅ Phone OTP (`/auth/phone-otp/*`)
- ✅ Email verification (`/auth/email-verify/*`)
- ✅ Password reset (`/auth/password-reset/*`)
- ✅ 2FA (`/auth/2fa/*`)

**Status:** ✅ Все реализовано в `apps/api-auth`

### ✅ Monolith (api-monolith, порт 7000)

**Должно быть в Monolith:**
- ✅ Управление профилем пользователя (`/api/v1/users/:userId`)
- ✅ Обновление профиля (`PUT /api/v1/users/:userId`)
- ✅ Загрузка аватара (`POST /api/v1/users/:userId/avatar`)
- ✅ Удаление профиля (`DELETE /api/v1/users/:userId`)
- ✅ Список users (`GET /api/v1/users`) - **ОТСУТСТВУЕТ!**
- ✅ Поиск users (`GET /api/v1/users/search`) - **ОТСУТСТВУЕТ!**

**Status:** ⚠️ Частично реализовано (4/6 эндпоинтов)

---

## 📋 Сравнение со спецификацией

### 1. Users Endpoints

| Спецификация | Текущее status | Статус |
|--------------|-------------------|--------|
| `GET /users/:userId` | ✅ `GET /api/v1/users/:userId` | ✅ Есть |
| `PUT /users/:userId` | ✅ `PUT /api/v1/users/:userId` | ✅ Есть |
| `POST /users/:userId/avatar` | ✅ `POST /api/v1/users/:userId/avatar` | ✅ Есть |
| `DELETE /users/:userId` | ✅ `DELETE /api/v1/users/:userId` | ✅ Есть |
| `GET /users` | ❌ Отсутствует | ❌ **НУЖНО ДОБАВИТЬ** |
| `GET /users/search?q=...` | ❌ Отсутствует | ❌ **НУЖНО ДОБАВИТЬ** |

**Вывод:** Не хватает 2 эндпоинтов for списка и поиска users.

---

### 2. Pipelines Endpoints

| Спецификация | Текущее status | Статус |
|--------------|-------------------|--------|
| `POST /pipelines` | ✅ `POST /api/v1/pipelines` | ✅ Есть |
| `GET /pipelines` | ✅ `GET /api/v1/pipelines` | ✅ Есть |
| `GET /pipelines/public` | ✅ `GET /api/v1/pipelines/marketplace/list` | ✅ Есть (other path) |
| `GET /pipelines/:id` | ✅ `GET /api/v1/pipelines/:id` | ✅ Есть |
| `PUT /pipelines/:id` | ✅ `PUT /api/v1/pipelines/:id` | ✅ Есть |
| `DELETE /pipelines/:id` | ✅ `DELETE /api/v1/pipelines/:id` | ✅ Есть |
| `POST /pipelines/:id/publish` | ❌ Отсутствует | ❌ **НУЖНО ДОБАВИТЬ** |
| `GET /pipelines/templates` | ❌ Отсутствует | ❌ **НУЖНО ДОБАВИТЬ** |
| `POST /pipelines/templates/:id/clone` | ❌ Отсутствует | ❌ **НУЖНО ДОБАВИТЬ** |
| `POST /executions` | ❌ Отсутствует | ❌ **НУЖНО ДОБАВИТЬ** |
| `GET /executions/:id` | ❌ Отсутствует | ❌ **НУЖНО ДОБАВИТЬ** |
| `GET /executions/:id/stats` | ❌ Отсутствует | ❌ **НУЖНО ДОБАВИТЬ** |

**Вывод:** Не хватает 6 эндпоинтов for публикации, templateов, клонирования и выполнения пайплайнов.

---

### 3. RBAC Endpoints

| Спецификация | Текущее status | Статус |
|--------------|-------------------|--------|
| `POST /rbac/roles` | ✅ `POST /api/v1/rbac/roles` | ✅ Есть |
| `GET /rbac/roles` | ✅ `GET /api/v1/rbac/roles` | ✅ Есть |
| `GET /rbac/roles/:id` | ✅ `GET /api/v1/rbac/roles/:id` | ✅ Есть |
| `PUT /rbac/roles/:id` | ✅ `PUT /api/v1/rbac/roles/:id` | ✅ Есть |
| `DELETE /rbac/roles/:id` | ✅ `DELETE /api/v1/rbac/roles/:id` | ✅ Есть |
| `POST /rbac/roles/assign` | ✅ `POST /api/v1/rbac/assign-role` | ✅ Есть (other path) |
| `POST /rbac/permissions` | ✅ `POST /api/v1/rbac/permissions` | ✅ Есть |
| `GET /rbac/permissions` | ✅ `GET /api/v1/rbac/permissions` | ✅ Есть |
| `GET /rbac/permissions/:id` | ❌ Отсутствует | ❌ **НУЖНО ДОБАВИТЬ** |
| `POST /rbac/permissions/grant` | ❌ Отсутствует | ❌ **НУЖНО ДОБАВИТЬ** |
| `DELETE /rbac/permissions/:roleId/:id` | ❌ Отсутствует | ❌ **НУЖНО ДОБАВИТЬ** |

**Вывод:** Не хватает 3 эндпоинтов for детальной работы с permissions.

---

### 4. Workers Endpoints

**Спецификация:** Не указана в API_GATEWAY_ENDPOINTS.md (Phase 0-2)

**Текущее status:** ✅ 13 эндпоинтов реализовано

**Вывод:** ✅ Реализовано, но не описано в спецификации Phase 0-2 (возможно Phase 6).

---

### 5. A/B Testing Endpoints

**Спецификация:** Не указана в API_GATEWAY_ENDPOINTS.md (Phase 0-2)

**Текущее status:** ✅ 8 эндпоинтов реализовано

**Вывод:** ✅ Реализовано, но не описано в спецификации Phase 0-2.

---

### 6. Integrations Endpoints

**Спецификация:** Не указана в API_GATEWAY_ENDPOINTS.md (Phase 0-2) - это Phase 3-5

**Текущее status:** ✅ 29 эндпоинтов реализовано

**Вывод:** ✅ Реализовано раньше спецификации (Phase 3-5).

---

### 7. Analytics Endpoints

**Спецификация:** Не указана в API_GATEWAY_ENDPOINTS.md (Phase 0-2)

**Текущее status:** ⚠️ 4 эндпоинта с TODO заглушками

**Вывод:** ⚠️ Реализовано partsчно (заглушки).

---

### 8. Generation Endpoints

**Спецификация:** Не указана в API_GATEWAY_ENDPOINTS.md (Phase 0-2)

**Текущее status:** ✅ 10 эндпоинтов реализовано

**Вывод:** ✅ Реализовано, но не описано в спецификации Phase 0-2.

---

## ❌ Отсутствующие эндпоинты по спецификации

### Users (2 эндпоинта)

1. ❌ `GET /api/v1/users` - список allх users с пагинацией
2. ❌ `GET /api/v1/users/search?q=...` - поиск users

### Pipelines (6 эндпоинтов)

3. ❌ `POST /api/v1/pipelines/:id/publish` - публикация пайплайна
4. ❌ `GET /api/v1/pipelines/templates` - список templateов
5. ❌ `POST /api/v1/pipelines/templates/:id/clone` - клонирование templateа
6. ❌ `POST /api/v1/executions` - launch выполнения пайплайна
7. ❌ `GET /api/v1/executions/:id` - получение выполнения
8. ❌ `GET /api/v1/executions/:id/stats` - statistics выполнения

### RBAC (3 эндпоинта)

9. ❌ `GET /api/v1/rbac/permissions/:id` - получение permission по ID
10. ❌ `POST /api/v1/rbac/permissions/grant` - выдача permission
11. ❌ `DELETE /api/v1/rbac/permissions/:roleId/:id` - отзыв permission

**Итого отсутствует:** 11 эндпоинтов из спецификации Phase 0-2

---

## ✅ Дополнительные эндпоинты (не в спецификации Phase 0-2)

### Workers (13 эндпоинтов)
- ✅ Реализовано, но не в спецификации Phase 0-2 (возможно Phase 6)

### A/B Testing (8 эндпоинтов)
- ✅ Реализовано, но не в спецификации Phase 0-2

### Integrations (29 эндпоинтов)
- ✅ Реализовано раньше спецификации (Phase 3-5)

### Analytics (4 эндпоинта)
- ⚠️ Реализовано partsчно (заглушки)

### Generation (10 эндпоинтов)
- ✅ Реализовано, но не в спецификации Phase 0-2

---

## 🚨 Критические проблемы

### 1. Отсутствие Swagger documentации (33 эндпоинта)

**Контроллеры without Swagger:**
- ❌ `IntegrationCrudController` - 13 эндпоинтов
- ❌ `EcommerceCrudController` - 7 эндпоинтов
- ❌ `AnalyticsController` - 4 эндпоинта
- ❌ `IntegrationMetricsController` - 3 эндпоинта
- ❌ `IntegrationHealthController` - 2 эндпоинта

### 2. Отсутствие авторизации (75 эндпоинтов)

**Контроллеры without авторизации:**
- ❌ `WorkersController` - 13 эндпоинтов
- ❌ `ABTestingController` - 8 эндпоинтов
- ❌ `IntegrationCrudController` - 13 эндпоинтов
- ❌ `EcommerceCrudController` - 7 эндпоинтов
- ❌ `AnalyticsController` - 4 эндпоинта
- ❌ `IntegrationMetricsController` - 3 эндпоинта
- ❌ `IntegrationHealthController` - 2 эндпоинта
- ❌ `IntegrationMonitoringController` - 3 эндпоинта
- ❌ `CredentialRotationController` - 4 эндпоинта
- ❌ `GenerationController` - 10 эндпоинтов

### 3. Несоresponseствие спецификации (11 эндпоинтов)

**Отсутствуют из спецификации Phase 0-2:**
- ❌ Users: 2 эндпоинта
- ❌ Pipelines: 6 эндпоинтов
- ❌ RBAC: 3 эндпоинта

### 4. Заглушки вместо реализации

**AnalyticsController:**
- ⚠️ Все 4 эндпоинта возвращают TODO заглушки

---

## 📝 Рекомендации

### 🔴 ВАЖНО: Правила NX projectа

**При добавлении новых эндпоинтов:**
- ✅ **Вся business-логика** должна быть в `libs/domain/*` или `libs/infrastructure/*`
- ✅ **Контроллеры** только в `apps/api-monolith` (вызывают methodы из libs)
- ✅ **Services, DTOs, Interfaces** - только в `libs/`
- ❌ **ЗАПРЕЩЕНО** добавлять business-логику напрямую в `apps/`

**Пример структуры:**
```
libs/domain/users/src/
├── services/
│   └── user.service.ts          ← Вся логика here
├── dto/
│   └── list-users.dto.ts        ← DTO here
└── interfaces/
    └── user-list.interface.ts   ← Интерфейсы here

apps/api-monolith/src/app/users/
└── users.controller.ts          ← Только HTTP маршруты, вызывает user.service
```

### Приоритет 1: Критические исправления

1. ✅ **Добавить авторизацию** for allх защищенных эндпоинтов (75 эндпоинтов)
2. ✅ **Добавить Swagger** for allх контроллеров without documentации (33 эндпоинта)
3. ✅ **Добавить отсутствующие эндпоинты** из спецификации (11 эндпоинтов)
   - ⚠️ **ВАЖНО**: Вся логика в `libs/domain/*`, контроллеры только в `apps/api-monolith`

### Приоритет 2: Уbetterния

4. ✅ **Реализовать Analytics** вместо TODO заглушек (4 эндпоинта)
5. ✅ **Улучшить Swagger** documentацию (parameterы, query, body)
6. ✅ **Добавить недостающие эндпоинты** for удобства (users/me, pipelines/execute)

### Приоритет 3: Дополнительно

7. ✅ **Добавить exampleы** в Swagger
8. ✅ **Добавить валидацию** via `class-validator`
9. ✅ **Добавить тесты** for allх новых эндпоинтов

---

## ✅ Итоговая statistics

| Категория | Всего | Реализовано | Отсутствует | Со Swagger | С Auth |
|-----------|-------|-------------|-------------|------------|--------|
| **Users** | 6 | 4 | 2 | 4 | 4 |
| **Pipelines** | 12 | 6 | 6 | 6 | 5 |
| **RBAC** | 11 | 8 | 3 | 8 | 8 |
| **Workers** | 13 | 13 | 0 | 13 | 0 |
| **A/B Testing** | 8 | 8 | 0 | 8 | 0 |
| **Integrations** | 29 | 29 | 0 | 0 | 0 |
| **Analytics** | 4 | 4 | 0 | 0 | 0 |
| **Generation** | 10 | 10 | 0 | 10 | 0 |
| **ИТОГО** | **93** | **82** | **11** | **49** | **17** |

---

**Status:** ⚠️ Требуется доработка for полного соresponseствия спецификации
