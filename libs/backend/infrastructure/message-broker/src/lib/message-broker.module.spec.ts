import { describe, it, expect } from 'vitest';
import { MessageBrokerModule } from './message-broker.module';

describe('MessageBrokerModule', () => {
  it('should be defined', () => {
    expect(MessageBrokerModule).toBeDefined();
  });
});
