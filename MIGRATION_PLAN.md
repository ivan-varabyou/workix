# 📋 План переноса libs под структуру NX

**Дата**: 2025-11-27

---

## 🎯 Цель

Адаптировать существующие библиотеки в `libs/domain/` под новую структуру с разделением на `entities/` и `features/`, сохраняя `domain/` как основную бизнес-логику.

---

## 📊 Анализ текущей структуры

### domain/ библиотеки

1. **domain/auth** - Аутентификация
   - ✅ Остается в domain/ (основная бизнес-логика)
   - ⚠️ Можно создать `entities/backend/account` для AccountLockEntity

2. **domain/users** - Пользователи
   - ✅ Остается в domain/ (основная бизнес-логика)
   - ✅ Уже создан `entities/backend/user` и `entities/frontend/user`

3. **domain/admin** - Администраторы
   - ✅ Остается в domain/ (основная бизнес-логика)
   - ⚠️ Можно создать `entities/backend/admin` для Admin entity

4. **domain/pipelines** - Пайплайны
   - ✅ Остается в domain/ (основная бизнес-логика)
   - ⚠️ Можно создать `entities/backend/pipeline` для Pipeline entity
   - ⚠️ Можно создать `features/backend/pipelines` для обертки сервисов

5. **domain/rbac** - Роли и права
   - ✅ Остается в domain/ (основная бизнес-логика)
   - ⚠️ Можно создать `entities/backend/role` для Role entity

6. **domain/workers** - Воркеры
   - ✅ Остается в domain/ (основная бизнес-логика)
   - ⚠️ Можно создать `entities/backend/worker` для Worker entity

7. **domain/workflows** - Воркфлоу
   - ✅ Остается в domain/ (основная бизнес-логика)
   - ⚠️ Можно создать `entities/backend/workflow` для Workflow entity

8. **domain/webhooks** - Webhooks
   - ✅ Остается в domain/ (основная бизнес-логика)

9. **domain/notifications** - Уведомления
   - ✅ Остается в domain/ (основная бизнес-логика)

---

## 🔄 Стратегия переноса

### Принцип: domain/ остается основным

- ✅ **domain/** - основная бизнес-логика (сервисы, модули, DTOs)
- ⚠️ **entities/** - чистые модели без зависимостей (создавать при необходимости)
- ⚠️ **features/** - обертки или UI компоненты (создавать при необходимости)

### Что переносить?

1. **Entities** - только если нужны чистые модели:
   - Модели без бизнес-логики
   - Модели для переиспользования в разных контекстах
   - Frontend модели отдельно от backend

2. **Features** - только если нужны обертки:
   - Обертки над domain сервисами
   - UI компоненты для frontend
   - Feature-specific логика

---

## 📋 План переноса (по приоритету)

### Этап 1: Entities для основных сущностей

1. ✅ `entities/backend/user` - уже создан
2. ✅ `entities/frontend/user` - уже создан
3. ⏳ `entities/backend/admin` - Admin entity
4. ⏳ `entities/backend/pipeline` - Pipeline entity
5. ⏳ `entities/backend/role` - Role entity (из RBAC)
6. ⏳ `entities/backend/worker` - Worker entity

### Этап 2: Features (опционально)

1. ✅ `features/backend/auth` - уже создан (пример)
2. ⏳ `features/backend/pipelines` - обертка над PipelineService (если нужна)
3. ⏳ `features/frontend/pipelines` - UI компоненты для пайплайнов

---

## 🚀 Начало переноса

Начинаем с создания entities для основных сущностей, которые используются в нескольких местах.
