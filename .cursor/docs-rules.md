# Правила создания и организации MD файлов для AI агента

**Версия**: 1.0
**Дата**: 2025-01-27

## 🎯 Основные правила

### ❌ ЗАПРЕЩЕНО создавать MD файлы в корне проекта

**Исключения** (только эти файлы могут быть в корне):
- `README.md` - главный README проекта
- `TASKS.md` - список задач проекта
- `SPECKIT_COMMANDS.md` - команды speckit

### ✅ Где создавать MD файлы

1. **Спецификации приложения** → `apps/{app-name}/.specs/`
   - **ВСЕ спецификации приложения хранятся здесь!**
   - `{APP}_PLAN.md` или `spec.md` (спецификация)
   - `{APP}_SECURITY.md` (кейсы безопасности)
   - `IMPLEMENTATION_PLAN.md` или `plan.md` (технический план)
   - `tasks.md` (задачи)
   - `ANALYSIS_REPORT.md` или `analysis.md` (анализ)
   - `SPECKIT_WORKFLOW.md` (инструкция по speckit)
   - `COMMANDS_ORDER.md` (краткая справка)
   - `checklists/` (чек-листы качества)

   **Правило**: Все документы, созданные командами `/speckit.*`, должны быть в `.specs/` соответствующего приложения!

2. **Системные спецификации** → `.specify/specs-optimized/`
   - Общие спецификации проекта
   - Правила разработки
   - Архитектурные решения

3. **Постоянная документация** → `.docs/`
   - `ARCHITECTURE_*.md` → `.docs/architecture/`
   - `GUIDE_*.md` → `.docs/guides/`
   - `API_*.md` → `.docs/api/`

4. **Временные отчеты** → `.docs/reports/`
   - `TEST_*.md`
   - `REPORT_*.md`
   - `CHECK_*.md`

## 🔄 После создания MD файла

**ОБЯЗАТЕЛЬНО** запустить:

```bash
make docs-organize
```

Это автоматически переместит файлы в правильные папки.

## 🧹 Регулярная очистка

После завершения операции запускать:

```bash
# Организовать новые файлы
make docs-organize

# Архивировать старые (>30 дней)
make docs-archive

# Очистить временные (>7 дней)
make docs-cleanup
```

## 📋 Классификация файлов

### Постоянные (остаются)
- `README.md` (в корне и приложениях)
- Архитектурная документация
- Руководства
- API документация

### Спецификации приложений (в `.specs/`)
- Планы реализации
- Спецификации безопасности
- Анализ согласованности
- Чек-листы

### Архивные (в `.docs/archive/`)
- Завершенные миграции
- Завершенные рефакторинги
- Старые анализы (>30 дней)
- Устаревшие планы (>30 дней)

### Временные (удаляются)
- Отчеты о тестировании (>7 дней)
- Временные отчеты (>7 дней)
- `TEMP_*.md` (удаляются сразу)

## 🚨 Критичные правила

1. **НЕ создавать** MD файлы в корне проекта (кроме исключений)
2. **ВСЕГДА** запускать `make docs-organize` после создания файлов
3. **РЕГУЛЯРНО** запускать `make docs-archive` и `make docs-cleanup`
4. **ИСПОЛЬЗОВАТЬ** `.specs/` для спецификаций приложений
5. **НЕ ДУБЛИРОВАТЬ** документацию в разных местах

## 📚 См. также

- [Полные правила организации документации](../.specify/docs-organization.md)
- [Структура .specs/](../.specs-template/README.md)
- [Документация проекта](../.docs/README.md)
