# Правила работы с Speckit командами для AI агента

**Версия**: 1.0
**Дата**: 2025-01-27

## 🎯 Основные правила

### Где хранятся спецификации

**КРИТИЧНО**: Все спецификации приложения хранятся в `apps/{app-name}/.specs/`

Пример для `api-admin`:
```
apps/api-admin/.specs/
├── README.md                    # Описание структуры
├── ADMIN_API_PLAN.md            # spec.md (спецификация от /speckit.specify)
├── IMPLEMENTATION_PLAN.md       # plan.md (технический план от /speckit.plan)
├── tasks.md                     # Задачи от /speckit.tasks
├── ADMIN_API_SECURITY.md        # Кейсы безопасности
├── ANALYSIS_REPORT.md           # Анализ от /speckit.analyze
├── SPECKIT_WORKFLOW.md          # Полная инструкция
├── COMMANDS_ORDER.md            # Краткая справка
└── checklists/
    └── requirements-quality.md  # Чек-листы от /speckit.checklist
```

## 📋 Правильный порядок команд

```
1. /speckit.specify      → Создает спецификацию в apps/{app}/.specs/{APP}_PLAN.md
2. /speckit.clarify      → Уточняет спецификацию (обновляет файл в .specs/)
3. /speckit.plan         → Создает технический план в apps/{app}/.specs/IMPLEMENTATION_PLAN.md
4. /speckit.tasks        → Создает задачи в apps/{app}/.specs/tasks.md
5. /speckit.checklist    → Создает чек-листы в apps/{app}/.specs/checklists/ (ПОСЛЕ tasks)
6. /speckit.analyze      → Создает анализ в apps/{app}/.specs/ANALYSIS_REPORT.md (ПОСЛЕ tasks)
7. /speckit.implement    → Реализация (опционально)
```

## ⚠️ Важные замечания

### 1. Порядок выполнения
- `/speckit.checklist` и `/speckit.analyze` должны выполняться **ПОСЛЕ** `/speckit.tasks`
- Не выполнять их до создания `tasks.md`

### 2. Где создавать файлы
- **ВСЕГДА** создавать спецификации в `apps/{app-name}/.specs/`
- **НЕ создавать** спецификации в корне проекта
- После создания запускать `make docs-organize` (хотя для `.specs/` это не обязательно, так как они уже в правильном месте)

### 3. Доступные команды

**Основные** (7 команд):
1. `specify` - Создание спецификации
2. `clarify` - Уточнение неоднозначностей
3. `plan` - Техническое планирование
4. `tasks` - Генерация задач
5. `checklist` - Чек-лист качества
6. `analyze` - Анализ согласованности
7. `implement` - Реализация

**Дополнительные** (3 команды):
8. `test` - ✅ Генерация тестового плана и проверка покрытия
9. `docs` - ✅ Генерация документации (API, guides, README)
10. `review` - ✅ Code review и проверка качества

**Release & Deploy** (2 команды):
11. `release` - ✅ Подготовка релиза (версионирование, changelog, теги)
12. `deploy` - ✅ Проверка готовности к деплою и генерация плана деплоя

**Важно**: `release` и `deploy` **НЕ выполняют** автоматический релиз/деплой, а только **готовят** все необходимое для ручного выполнения.

**Не существуют**:
- `monitor`, `optimize`, `refactor`, `cleanup`, `support`, `improve`

**Всего доступно**: 12 команд

## 🔄 Workflow для каждого приложения

1. **Определить приложение**: `apps/{app-name}/`
2. **Создать `.specs/` папку** (если не существует)
3. **Выполнить команды speckit** в правильном порядке
4. **Все файлы автоматически сохраняются** в `apps/{app-name}/.specs/`

## 📚 См. также

- [Полная инструкция по speckit](../apps/api-admin/.specs/SPECKIT_WORKFLOW.md)
- [Краткая справка](../apps/api-admin/.specs/COMMANDS_ORDER.md)
- [Правила организации MD файлов](./docs-rules.md)
- [Шаблон для других apps](../.specs-template/README.md)
