# 🚀 Быстрый start DevOps Agent

## ✅ Что already ready

- ✅ Python 3.10.12 установлен
- ✅ Виртуальное окрalreadyние `.venv` создано
- ✅ AutoGen, LangChain, Ollama установлены
- ✅ Файл `devops_agent.py` создан и configured
- ⏳ Модель `qwen2.5:7b` скачивается (может занять время)

## 🎯 Быстрый launch

### 1. Проверьте, what model скачана

```bash
ollama list
```

Должна быть model `qwen2.5:7b`. Если нет - дождитесь завершения загрузки или запустите:

```bash
ollama pull qwen2.5:7b
```

### 2. Активируйте виртуальное окрalreadyние

```bash
source .venv/bin/activate
```

### 3. Запустите агента

```bash
python agents/devops_agent.py
```

Или используйте в Python коде:

```python
from agents.devops_agent import user, coder, tester

# Запуск чата с агентом-кодером
user.initiate_chat(
    coder,
    message="Создай new service for авторизации"
)
```

## 🧪 Тестирование underключения

Перед usageм проверьте connection:

```bash
source .venv/bin/activate
python scripts/test_ollama_autogen.py
```

## 📝 Примеры usage

### Пример 1: Простой request к агенту

```python
from agents.devops_agent import user, coder

user.initiate_chat(
    coder,
    message="Напиши функцию на Python for вычисления фаwhoриала"
)
```

### Пример 2: Usage тестового агента

```python
from agents.devops_agent import user, tester

user.initiate_chat(
    tester,
    message="Создай unit-тесты for функции вычисления фаwhoриала"
)
```

## ⚠️ Важные замечания

1. **Модель скачивается**: Процесс может занять 30-60 минут в зависимости от скорости интернета
2. **Ollama должен быть запущен**: Убедитесь, what `ollama serve` работает
3. **Виртуальное окрalreadyние**: Всегда активируйте `.venv` before usageм

## 🔧 Решение проблем

### Проблема: Модель не found

```bash
# Проверьте список моделей
ollama list

# Если модели нет, скачайте её
ollama pull qwen2.5:7b
```

### Проблема: Ollama не отвечает

```bash
# Проверьте, what Ollama запущен
pgrep -f ollama

# Если не запущен, запустите
ollama serve
```

### Проблема: AutoGen не может underключиться

```bash
# Проверьте connection вручную
curl http://localhost:11434/api/tags

# Должен вернуть список моделей
```

## 📚 Дополнительная documentация

- Полная instruction: `.specify/specs/000-project/OLLAMA_AUTOGEN_SETUP.md`
- README агента: `agents/README.md`


