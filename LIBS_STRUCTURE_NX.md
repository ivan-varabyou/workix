# 📁 Структура libs по рекомендациям NX с разделением backend/frontend

**Дата**: 2025-11-27

---

## 🎯 Рекомендуемая структура NX с разделением

```
libs/
├── shared/
│   ├── utils/                 # ✅ Общие утилиты
│   ├── backend/
│   │   ├── config/            # ✅ Backend конфигурация
│   │   ├── api/               # ✅ Backend API клиент
│   │   └── core/              # ✅ Backend core
│   └── frontend/
│       ├── api/               # ✅ Frontend API клиент
│       ├── core/              # ✅ Frontend core
│       └── ui/                # ✅ Frontend UI компоненты
│
├── entities/
│   ├── backend/
│   │   ├── user/              # Backend сущность пользователя
│   │   └── product/           # Backend сущность продукта
│   └── frontend/
│       ├── user/              # Frontend модель пользователя
│       └── product/           # Frontend модель продукта
│
├── features/
│   ├── backend/
│   │   ├── auth/              # Backend фича авторизации
│   │   ├── cart/              # Backend фича корзины
│   │   └── checkout/          # Backend фича оформления заказа
│   └── frontend/
│       ├── auth/              # Frontend фича авторизации
│       ├── cart/              # Frontend фича корзины
│       └── checkout/          # Frontend фича оформления заказа
│
├── domain/                    # ✅ Бизнес-логика (остается)
│   ├── auth/
│   ├── users/
│   └── ...
│
├── infrastructure/            # ✅ Инфраструктура (остается)
│   └── ...
│
├── integrations/              # ✅ Интеграции (остается)
│   └── ...
│
├── ai/                        # ✅ AI (остается)
│   └── ...
│
└── utilities/                 # ✅ Утилиты (остается)
    └── ...
```

---

## 📋 Разделение entities

### entities/backend/ - Backend сущности

**Назначение:**
- Модели данных с бизнес-логикой
- Валидация данных
- Связи с БД (Prisma)
- Репозитории и data access

**Пример:**
```
entities/backend/user/
├── src/
│   ├── model/
│   │   ├── user.entity.ts      # Entity класс с методами
│   │   └── user.interface.ts   # Интерфейсы
│   ├── repository/
│   │   └── user.repository.ts  # Data access
│   └── index.ts
```

### entities/frontend/ - Frontend сущности

**Назначение:**
- Модели для UI
- DTOs для форм
- Типы для API responses
- Валидация форм

**Пример:**
```
entities/frontend/user/
├── src/
│   ├── model/
│   │   ├── user.model.ts       # Frontend модель
│   │   └── user.interface.ts   # Интерфейсы для UI
│   ├── api/
│   │   └── user.api.ts         # API клиент
│   └── index.ts
```

---

## 📋 Разделение features

### features/backend/ - Backend фичи

**Назначение:**
- Сервисы с бизнес-логикой
- Контроллеры (если нужны)
- Guards, Interceptors
- Валидация и обработка данных

**Пример:**
```
features/backend/auth/
├── src/
│   ├── services/
│   │   └── auth.service.ts
│   ├── controllers/
│   │   └── auth.controller.ts
│   ├── guards/
│   │   └── auth.guard.ts
│   └── index.ts
```

### features/frontend/ - Frontend фичи

**Назначение:**
- UI компоненты
- Хуки (React/Angular)
- Состояние (state management)
- Формы и валидация

**Пример:**
```
features/frontend/auth/
├── src/
│   ├── components/
│   │   ├── login/
│   │   └── register/
│   ├── hooks/
│   │   └── use-auth.hook.ts
│   └── index.ts
```

---

## 🔄 Соответствие с domain/

### domain/ vs features/backend/

**domain/** - Доменная бизнес-логика:
- ✅ `domain/auth/` - полная бизнес-логика авторизации
- ✅ `domain/users/` - бизнес-логика пользователей
- ✅ Используется в backend приложениях

**features/backend/** - Backend фичи (опционально):
- ⚠️ Можно использовать `domain/auth/` напрямую
- ⚠️ Или создать `features/backend/auth/` как обертку над `domain/auth/`

**Рекомендация:**
- Использовать `domain/*` для бизнес-логики
- `features/backend/*` создавать только если нужна дополнительная обертка или специфичная логика

---

## ✅ Итоговая структура

```
libs/
├── shared/
│   ├── utils/                 # ✅ Общие утилиты
│   ├── backend/               # ✅ Backend библиотеки
│   └── frontend/              # ✅ Frontend библиотеки
│
├── entities/
│   ├── backend/               # Backend сущности (если нужны)
│   │   ├── user/
│   │   └── product/
│   └── frontend/              # Frontend модели (если нужны)
│       ├── user/
│       └── product/
│
├── features/
│   ├── backend/               # Backend фичи (если нужны)
│   │   ├── auth/
│   │   ├── cart/
│   │   └── checkout/
│   └── frontend/              # Frontend фичи (если нужны)
│       ├── auth/
│       ├── cart/
│       └── checkout/
│
├── domain/                    # ✅ Бизнес-логика (основная)
│   ├── auth/
│   ├── users/
│   └── ...
│
├── infrastructure/            # ✅ Инфраструктура
│   └── ...
│
├── integrations/              # ✅ Интеграции
│   └── ...
│
├── ai/                        # ✅ AI
│   └── ...
│
└── utilities/                 # ✅ Утилиты
    └── ...
```

---

## 🎯 Рекомендации

### 1. Использовать существующие domain/
- ✅ `domain/auth/` - для backend бизнес-логики
- ✅ `domain/users/` - для backend бизнес-логики пользователей

### 2. Создавать entities/ только при необходимости
- ⚠️ `entities/backend/user/` - только если нужна чистая модель (без бизнес-логики)
- ⚠️ `entities/frontend/user/` - только если нужна frontend модель (отдельно от domain)

### 3. Создавать features/ только при необходимости
- ⚠️ `features/backend/auth/` - только если нужна обертка над `domain/auth/`
- ⚠️ `features/frontend/auth/` - только если нужны frontend компоненты авторизации

### 4. Приоритет использования
1. **domain/** - основная бизнес-логика (использовать в первую очередь)
2. **shared/** - общие компоненты
3. **entities/** - модели данных (если нужны отдельно)
4. **features/** - фичи (если нужны обертки или UI компоненты)
