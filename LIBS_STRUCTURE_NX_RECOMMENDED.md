# 📁 Структура libs по рекомендациям NX

**Дата**: 2025-11-27

---

## 🎯 Рекомендуемая структура NX

```
libs/
├── shared/                    # Общие библиотеки
│   ├── utils/                 # ✅ Создана
│   ├── config/                # ⚠️ Нужно: общая конфигурация (не только backend)
│   ├── api/                   # ⚠️ Нужно: общий API клиент (интерфейсы)
│   │
│   ├── backend/               # Backend-специфичные
│   │   ├── config/            # ✅ Backend конфигурация
│   │   ├── api/               # ✅ Backend API клиент
│   │   └── core/              # ✅ Backend core
│   │
│   └── frontend/              # Frontend-специфичные
│       ├── api/               # ✅ Frontend API клиент
│       ├── core/              # ✅ Frontend core
│       └── ui/                # ✅ Frontend UI компоненты
│
├── ui/                        # UI компоненты (опционально, если нужны отдельно)
│   ├── button/
│   └── modal/
│
├── entities/                  # Сущности (опционально, если нужны)
│   ├── user/
│   └── product/
│
├── features/                  # Фичи (опционально, если нужны)
│   ├── auth/
│   ├── cart/
│   └── checkout/
│
├── domain/                    # ✅ Существует - бизнес-логика
│   ├── auth/
│   ├── users/
│   ├── pipelines/
│   └── ...
│
├── infrastructure/            # ✅ Существует - инфраструктура
│   ├── database/
│   ├── prisma/
│   └── ...
│
├── integrations/              # ✅ Существует - интеграции
│   ├── cloud/
│   ├── code/
│   └── ...
│
├── ai/                        # ✅ Существует - AI
│   └── ...
│
└── utilities/                  # ✅ Существует - утилиты
    └── ...
```

---

## 📋 Адаптация текущей структуры

### 1. shared/ - Общие библиотеки

**Текущее состояние:**
- ✅ `shared/utils/` - создана
- ✅ `shared/backend/config/` - создана
- ✅ `shared/backend/api/` - создана
- ✅ `shared/frontend/api/` - создана

**Рекомендации NX:**
- ⚠️ `shared/config/` - общая конфигурация (если нужна общая для backend и frontend)
- ⚠️ `shared/api/` - общие интерфейсы API (если нужны общие типы)

**Решение:**
- Оставить текущую структуру с разделением backend/frontend
- Если нужны общие интерфейсы, создать `shared/api/` только с типами/интерфейсами

---

### 2. ui/ - UI компоненты

**Текущее состояние:**
- ✅ `shared/frontend/ui/` - существует с множеством компонентов

**Рекомендации NX:**
- `ui/button/` - отдельная библиотека для button
- `ui/modal/` - отдельная библиотека для modal

**Решение:**
- Если нужна более модульная структура, можно вынести отдельные компоненты
- Или оставить в `shared/frontend/ui/` (текущий подход)

---

### 3. entities/ - Сущности

**Текущее состояние:**
- ✅ `domain/users/` - существует
- ✅ `domain/auth/` - существует

**Рекомендации NX:**
- `entities/user/` - сущность пользователя
- `entities/product/` - сущность продукта

**Решение:**
- Использовать существующие `domain/*` библиотеки
- Или создать `entities/` для чистой модели данных (без бизнес-логики)

---

### 4. features/ - Фичи

**Текущее состояние:**
- ✅ `domain/auth/` - существует
- ❌ `features/cart/` - не существует
- ❌ `features/checkout/` - не существует

**Рекомендации NX:**
- `features/auth/` - фича авторизации
- `features/cart/` - фича корзины
- `features/checkout/` - фича оформления заказа

**Решение:**
- Использовать `domain/auth/` для backend логики
- Создать `features/auth/` для frontend компонентов (если нужны)
- Создать `features/cart/` и `features/checkout/` если нужны

---

## ✅ Итоговая адаптированная структура

```
libs/
├── shared/
│   ├── utils/                 # ✅ Общие утилиты
│   ├── backend/
│   │   ├── config/            # ✅ Backend конфигурация
│   │   ├── api/               # ✅ Backend API клиент
│   │   └── core/              # ✅ Backend core
│   └── frontend/
│       ├── api/               # ✅ Frontend API клиент
│       ├── core/              # ✅ Frontend core
│       └── ui/                # ✅ Frontend UI
│
├── domain/                    # ✅ Бизнес-логика (остается)
│   ├── auth/
│   ├── users/
│   └── ...
│
├── infrastructure/            # ✅ Инфраструктура (остается)
│   └── ...
│
├── integrations/              # ✅ Интеграции (остается)
│   └── ...
│
├── ai/                        # ✅ AI (остается)
│   └── ...
│
└── utilities/                  # ✅ Утилиты (остается)
    └── ...
```

---

## 🎯 Рекомендации

1. **Оставить текущую структуру** - она уже хорошо организована
2. **Добавить только при необходимости:**
   - `ui/button`, `ui/modal` - только если нужна более модульная структура
   - `entities/user`, `entities/product` - только если нужны чистые модели данных
   - `features/auth`, `features/cart`, `features/checkout` - только если нужны frontend фичи

3. **Использовать существующие:**
   - `domain/*` для бизнес-логики
   - `shared/frontend/ui/` для UI компонентов
   - `shared/backend/core/` для backend core

---

## 📝 Теги NX

Текущие теги соответствуют рекомендациям:
- `type:lib` - тип библиотеки
- `scope:shared` - область (shared, domain, infrastructure, etc.)
- `type:frontend` / `type:backend` - дополнительная категоризация
