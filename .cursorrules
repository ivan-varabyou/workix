# Cursor Rules for Workix Platform

## 🎯 Основной контекст

**ВАЖНО**: Всегда начинай с чтения `.specify/specs-optimized/index.md` для понимания структуры проекта!

## 📚 Главные спецификации

1. **Начало работы**: `.specify/specs-optimized/index.md` - **ОБЯЗАТЕЛЬНО ПРОЧИТАТЬ ПЕРВЫМ!**
2. **Процесс разработки**: `.specify/specs-optimized/core/development.md`
3. **Правила разработки**: `.specify/specs/005-development-process/DEVELOPMENT_RULES.md` (detailed)
4. **Git Workflow**: `.specify/specs-optimized/core/git-workflow.md`
5. **Тестирование**: `.specify/specs-optimized/process/testing.md`

## 🔴 Критичные правила

### 0. Language & Documentation
- ✅ **ALL code, comments, documentation, specifications MUST be in English**
- ✅ Write concisely, to the point - avoid unnecessary context
- ✅ Comments only when code intent is not obvious

### 1. MCP Servers (ALWAYS FIRST!)
- ✅ **REQUIRED**: `make mcp-status` before starting work
- ✅ If not running: `make mcp-start`
- ✅ Available: Workix MCP, Ollama, TypeScript, NX, ESLint servers

### 2. Work only on task branch!
- ❌ DO NOT switch branches during development
- ✅ Work only on task branch (task-{number})
- ✅ All changes only on current task branch

### 3. Testing Shared Libraries
- ✅ Minimum 85% coverage for shared libraries
- ✅ Vitest for testing
- ✅ Storybook for UI components

### 4. i18n Usage
- ❌ DO NOT use hardcoded text
- ✅ Use I18nService for all texts

## 📋 Commit Format

`T #{number} - {type}({scope}): description`

- ✅ Always create branches from `develop`
- ✅ **ALL commits: English only, lowercase**
- ✅ Specify scope in parentheses

## 🎯 Структура проекта

### Организация документации
- ✅ **КРИТИЧНО**: НЕ создавать MD файлы в корне проекта (кроме README.md, TASKS.md, SPECKIT_COMMANDS.md)
- ✅ Спецификации приложений → `apps/{app-name}/.specs/`
- ✅ Системные спецификации → `.specify/specs-optimized/`
- ✅ Постоянная документация → `.docs/`
- ✅ Временные отчеты → `.docs/reports/` (автоматически архивируются)
- ✅ **ВСЕГДА** запускать `make docs-organize` после создания MD файлов
- ✅ См. `.cursor/docs-rules.md` для детальных правил

### Структура библиотек
```
libs/
├── domain/                    # Доменные библиотеки
├── infrastructure/            # Инфраструктурные библиотеки
├── integrations/              # Интеграции
├── ai/                        # AI библиотеки
├── shared/                    # Общие библиотеки
│   ├── frontend/
│   │   ├── core/              # Frontend core
│   │   └── ui/                # UI компоненты
│   └── backend/
│       └── core/              # Backend core
└── utilities/                 # Утилиты
```

## ✅ Pre-work Checklist

1. ✅ Read `.specify/specs-optimized/index.md`
2. ✅ Check current branch (should be task-{number})
3. ✅ Read `.specify/specs-optimized/core/development.md`
4. ✅ Follow development rules
5. ✅ Use correct commit format
6. ✅ DO NOT switch branches during development
7. ✅ **All code/docs in English, concise**

## 🔄 Auto-continue Work

**IMPORTANT**: When work stops, use command for auto-continue:

```bash
# Auto-create command and copy to clipboard
make copy-continue

# Or show command
make auto-continue-exec
```

**Rule**: After task completion or work stop, auto-run `make copy-continue` and use command from `.cursor/trigger-continue.txt` to continue.

## 💡 Context Efficiency

### For English requests:
- Provide **concise information** to save tokens
- Use brief answers and bullet points
- Minimize repetitions

### Summary:
- **Always** output brief summary in Russian
- What was done very briefly (2-3 lines)
- Key changes in one line

## 📁 MD Files Organization

**CRITICAL**: After creating any MD file, run `make docs-organize`

- ❌ **FORBIDDEN** to create MD files in project root (except README.md, TASKS.md, SPECKIT_COMMANDS.md)
- ✅ **ALL app specifications** → `apps/{app-name}/.specs/` (all files from `/speckit.*` commands)
- ✅ Permanent documentation → `.docs/`
- ✅ Temporary reports → `.docs/reports/`
- ✅ See `.cursor/docs-rules.md` for detailed rules
- ✅ See `.cursor/speckit-rules.md` for speckit command rules

## 🔗 Full Context

For full context see:
- `.cursor/MCP_SUMMARY_RU.md` - **Brief MCP servers summary**
- `.specify/AI_CONTEXT.md` - Full AI agent context
- `.specify/specs-optimized/index.md` - Main index of all specifications
- `.cursor/README.md` - Auto-continue work instructions
- `.cursor/docs-rules.md` - **MD files organization rules**
