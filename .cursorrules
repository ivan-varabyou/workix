# Cursor Rules for Workix Platform

## 🎯 Основной контекст

**ВАЖНО**: Всегда начинай с чтения `.specify/specs-optimized/index.md` для понимания структуры проекта!

## 📚 Главные спецификации

1. **Начало работы**: `.specify/specs-optimized/index.md` - **ОБЯЗАТЕЛЬНО ПРОЧИТАТЬ ПЕРВЫМ!**
2. **Процесс разработки**: `.specify/specs-optimized/core/development.md`
3. **Правила разработки**: `.specify/specs/005-development-process/DEVELOPMENT_RULES.md` (detailed)
4. **Git Workflow**: `.specify/specs-optimized/core/git-workflow.md`
5. **Тестирование**: `.specify/specs-optimized/process/testing.md`

## 🔴 Критичные правила

### 0. MCP Серверы (ВСЕГДА ПЕРВЫМ!)
- ✅ **ОБЯЗАТЕЛЬНО**: `make mcp-status` перед началом работы
- ✅ Если не запущены: `make mcp-start`
- ✅ Доступны: Workix MCP, Ollama, TypeScript, NX, ESLint серверы

### 1. Работа только на своей ветке задачи!
- ❌ НЕ переключаться на другие ветки во время разработки
- ✅ Работать только на своей ветке задачи (task-{номер})
- ✅ Все изменения делать только на текущей ветке задачи

### 2. Тестирование Shared библиотек
- ✅ Минимум 85% покрытие для shared библиотек
- ✅ Vitest для тестирования
- ✅ Storybook для UI компонентов

### 3. Использование i18n
- ❌ НЕ использовать хардкод текста
- ✅ Использовать I18nService для всех текстов

## 📋 Формат коммитов

`T #{номер} - {тип}({область}): описание`

- ✅ Всегда создавать ветки от `develop`
- ✅ **ВСЕ коммиты: только английский язык и нижний регистр (lowercase)**
- ✅ Указывать область (scope) в скобках

## 🎯 Структура проекта

### Организация документации
- ✅ Все MD файлы проекта → `.specify/specs/000-project/`
- ✅ README.md остается в корне проекта
- ✅ Спецификации остаются в своих папках (`.specify/specs/XXX-*/`)

### Структура библиотек
```
libs/
├── domain/                    # Доменные библиотеки
├── infrastructure/            # Инфраструктурные библиотеки
├── integrations/              # Интеграции
├── ai/                        # AI библиотеки
├── shared/                    # Общие библиотеки
│   ├── frontend/
│   │   ├── core/              # Frontend core
│   │   └── ui/                # UI компоненты
│   └── backend/
│       └── core/              # Backend core
└── utilities/                 # Утилиты
```

## ✅ Чек-лист перед началом работы

1. ✅ Прочитать `.specify/specs-optimized/index.md`
2. ✅ Проверить текущую ветку (должна быть task-{номер})
3. ✅ Прочитать `.specify/specs-optimized/core/development.md`
4. ✅ Следовать правилам разработки
5. ✅ Использовать правильный формат коммитов
6. ✅ НЕ переключаться на другие ветки во время разработки

## 🔄 Автоматическое продолжение работы

**ВАЖНО**: Когда работа останавливается, используй команду для автоматического продолжения:

```bash
# Автоматически создать команду и скопировать в буфер обмена
make copy-continue

# Или показать команду
make auto-continue-exec
```

**Правило**: После завершения задачи или при остановке работы, автоматически запускай `make copy-continue` и используй команду из `.cursor/trigger-continue.txt` для продолжения.

## 💡 Экономия контекста

### Для английских запросов:
- Давать **сжатую информацию** для экономии токенов
- Использовать краткие ответы и bullet points
- Минимизировать повторения

### Саммари:
- **Всегда** выводить краткий саммари на русском
- Что сделано очень кратко (2-3 строки)
- Ключевые изменения одной строкой

## 🔗 Полный контекст

Для полного контекста смотри:
- `.cursor/MCP_SUMMARY_RU.md` - **Краткий саммари MCP серверов**
- `.specify/AI_CONTEXT.md` - Полный контекст AI агента
- `.specify/specs-optimized/index.md` - Главный индекс всех спецификаций
- `.cursor/README.md` - Инструкции по автоматическому продолжению работы
