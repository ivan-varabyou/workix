# 🏗️ Workix Platform - Итоговая архитектура приложения

## 📊 Общая схема платформы

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           WORKIX PLATFORM                                   │
│                         Visual Automation Platform                          │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                              FRONTEND LAYER                                 │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────┐              ┌──────────────────────┐
│   app-admin          │              │   app-web            │
│   (Admin Panel)      │              │   (Web App)          │
│                      │              │                      │
│   Port: 7300         │              │   Port: 7301         │
│   Angular + PrimeNG  │              │   Angular + PrimeNG │
│   Zoneless           │              │   Zoneless           │
└──────────┬───────────┘              └──────────┬───────────┘
           │                                     │
           │ HTTP/HTTPS                          │ HTTP/HTTPS
           │                                     │
           └──────────────┬──────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                            API GATEWAY LAYER                                │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                        api-gateway                                          │
│                        (Routing & Load Balancing)                           │
│                                                                              │
│                        Port: 7100                                           │
│                        Features:                                            │
│                        - Request routing                                    │
│                        - Authentication & authorization                     │
│                        - Rate limiting                                      │
│                        - Health checks                                      │
│                        - CORS configuration                                 │
└────────────────────────────┬────────────────────────────────────────────────┘
                             │
                             │ Routes requests
                             │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│  api-monolith    │  │  api-auth        │  │ api-notifications│
│  (Monolith API)  │  │  (Auth Service)  │  │ (Notifications)  │
│                  │  │                  │  │                  │
│  Port: 7000      │  │  Port: 7200      │  │  Port: 7201      │
│  Swagger: /docs  │  │  Swagger: /docs  │  │  Swagger: /docs  │
└────────┬─────────┘  └────────┬─────────┘  └────────┬─────────┘
         │                     │                     │
         │                     │                     │
         │                     │                     │
         ▼                     ▼                     ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                            DATABASE LAYER                                   │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│ postgres-monolith│  │  postgres-auth    │  │postgres-notifications│
│                  │  │                  │  │                  │
│  Port: 5000      │  │  Port: 5200      │  │  Port: 5201      │
│  DB: workix_     │  │  DB: workix_auth │  │  DB: workix_     │
│     monolith     │  │                  │  │     notifications │
│                  │  │  Models:         │  │                  │
│  Models:         │  │  - User          │  │  Models:         │
│  - Pipeline      │  │  - SocialAccount │  │  - (Future)      │
│  - Execution     │  │  - PhoneOtp      │  │                  │
│  - Worker        │  │  - EmailVerif    │  │                  │
│  - Integration   │  │  - RefreshToken   │  │                  │
│  - Webhook       │  │  - PasswordReset │  │                  │
│  - RBAC          │  │  - PushSubscription│ │                  │
│  - AuditLog      │  │                  │  │                  │
└──────────────────┘  └──────────────────┘  └──────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                         MESSAGE QUEUE LAYER                                 │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                              Redis                                          │
│                              (Message Queue)                                │
│                                                                              │
│                              Port: 5900                                     │
│                              Queues:                                        │
│                              - notifications:email                           │
│                              - notifications:push                           │
└────────────────────────────┬────────────────────────────────────────────────┘
                             │
                             │ Consumes events
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                      api-notifications (Worker)                             │
│                      (Background Processing)                                 │
│                                                                              │
│                      Processors:                                            │
│                      - EmailProcessor                                       │
│                      - PushProcessor                                        │
│                                                                              │
│                      Services:                                              │
│                      - EmailNotificationService (SendGrid)                  │
│                      - PushNotificationService (Web Push)                   │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                            UTILITY SERVICES                                 │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                        mcp-server                                           │
│                        (MCP Protocol Server)                                 │
│                                                                              │
│                        Transport: stdio                                     │
│                        Purpose: AI agent integration                        │
│                        Features:                                            │
│                        - Resources (documentation)                           │
│                        - Tools (API methods)                                │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 📋 Детальная схема сервисов

### 🎨 Frontend Applications

#### app-admin (Port 7300)
- **Технологии**: Angular, PrimeNG, Zoneless
- **Назначение**: Административная панель
- **Endpoints**: `http://localhost:7300`
- **API**: Подключается к `api-gateway` (7100)

#### app-web (Port 7301)
- **Технологии**: Angular, PrimeNG, Zoneless
- **Назначение**: Клиентское веб-приложение
- **Endpoints**: `http://localhost:7301`
- **API**: Подключается к `api-gateway` (7100)
- **Features**: Service Worker для push-уведомлений

### 🔌 Backend Services

#### api-gateway (Port 7100)
- **Назначение**: Единая точка входа, маршрутизация запросов
- **Endpoints**: `http://localhost:7100`
- **Routing**:
  - `/api/v1/auth/*` → `api-auth` (7200) → `/api-auth/v1/auth/*`
  - `/api/v1/users/*` → `api-auth` (7200) → `/api-auth/v1/users/*`
  - `/api/v1/notifications/*` → `api-notifications` (7201) → `/api-notifications/v1/notifications/*`
  - `/api/v1/subscriptions/*` → `api-notifications` (7201) → `/api-notifications/v1/subscriptions/*`
  - `/api/v1/pipelines/*` → `api-monolith` (7000) → `/api-monolith/v1/pipelines/*`
  - `/api/v1/*` → `api-monolith` (7000) → `/api-monolith/v1/*`
- **Features**:
  - Authentication & authorization
  - Rate limiting
  - Health checks
  - Request/response logging
  - CORS configuration

#### api-monolith (Port 7000)
- **Назначение**: Монолитное API со всеми сервисами (Phase 1)
- **Endpoints**: `http://localhost:7000`
- **Swagger**: `http://localhost:7000/api-monolith/docs`
- **Database**: `postgres-monolith` (5000)
- **Modules**:
  - PipelinesModule
  - ExecutionsModule
  - RBACModule
  - AuditLogsModule
  - WebhooksModule
  - ApiKeysModule
  - WorkflowsModule
  - WorkersModule
  - AICoreModule
  - GenerationModule
  - IntegrationCoreModule
  - IntegrationsModule
  - ABTestingModule

#### api-auth (Port 7200)
- **Назначение**: Микросервис аутентификации
- **Endpoints**: `http://localhost:7200`
- **Swagger**: `http://localhost:7200/docs`
- **Database**: `postgres-auth` (5200)
- **Features**:
  - User registration & login
  - JWT tokens (access & refresh)
  - Password reset
  - Two-factor authentication (2FA/TOTP)
  - OAuth2 (Google, GitHub, Apple)
  - Email verification
  - Phone OTP authentication
  - Security code verification
  - User profile management
- **Controllers**:
  - AuthController
  - OAuth2Controller
  - PhoneOtpController
  - EmailVerificationController
  - AuthSecurityController
  - UsersController

#### api-notifications (Port 7201) - Гибридный сервис
- **Назначение**: Сервис уведомлений (HTTP API + Worker)
- **Endpoints**: `http://localhost:7201`
- **Swagger**: `http://localhost:7201/docs`
- **Database**:
  - `postgres-notifications` (5201) - для метаданных
  - `postgres-auth` (5200) - для PushSubscription (чтение/запись)
- **HTTP API**:
  - `POST /api-notifications/v1/subscriptions` - Регистрация push подписки
  - `GET /api-notifications/v1/subscriptions` - Получение подписок
  - `DELETE /api-notifications/v1/subscriptions/:id` - Удаление подписки
- **Worker** (Background Processing):
  - EmailProcessor - обработка email событий
  - PushProcessor - обработка push событий
- **Message Queues**:
  - `notifications:email` - очередь для email
  - `notifications:push` - очередь для push
- **Services**:
  - EmailNotificationService (SendGrid)
  - PushNotificationService (Web Push API)

### 🗄️ Databases

#### postgres-monolith (Port 5000)
- **Database**: `workix_monolith`
- **Models**: Pipeline, Execution, Worker, Integration, Webhook, RBAC, AuditLog, etc.
- **Used by**: `api-monolith`

#### postgres-auth (Port 5200)
- **Database**: `workix_auth`
- **Models**:
  - User, SocialAccount, PhoneOtp, EmailVerification
  - RefreshToken, PasswordReset, IpBlock
  - AccountSecurityEvent, AccountSecurityStatus
  - SuspiciousIpActivity, SecurityVerificationCode
  - **PushSubscription** (используется `api-notifications`)
- **Used by**:
  - `api-auth` (основной доступ)
  - `api-notifications` (PushSubscription - чтение/запись)

#### postgres-notifications (Port 5201)
- **Database**: `workix_notifications`
- **Models**: (Future - для метаданных уведомлений)
- **Used by**: `api-notifications`

### 📨 Message Queue

#### Redis (Port 5900)
- **Назначение**: Очереди сообщений для асинхронной обработки
- **Queues**:
  - `notifications:email` - события email уведомлений
  - `notifications:push` - события push уведомлений
- **Used by**:
  - `api-auth` - публикация событий
  - `api-notifications` - обработка событий

### 🛠️ Utility Services

#### mcp-server
- **Назначение**: Model Context Protocol сервер для AI агентов
- **Transport**: stdio
- **Features**:
  - Resources (документация проекта)
  - Tools (API методы для работы с проектом)
  - Integration с AI агентами (Cursor, Windsurf)

## 🔄 Потоки данных

### 1. Аутентификация пользователя

```
User → app-web (7301)
  → api-gateway (7100)
    → api-auth (7200)
      → postgres-auth (5200)
        → JWT Token
          → User
```

### 2. Регистрация Push подписки

```
User → app-web (7301)
  → api-gateway (7100)
    → api-notifications (7201) HTTP API
      → postgres-auth (5200) PushSubscription
        → Success
```

### 3. Отправка Email уведомления

```
api-auth (7200)
  → EventPublisherService
    → Redis (5900) notifications:email
      → api-notifications (7201) Worker
        → EmailProcessor
          → EmailNotificationService
            → SendGrid
              → User Email
```

### 4. Отправка Push уведомления

```
api-auth (7200) или другой сервис
  → EventPublisherService
    → Redis (5900) notifications:push
      → api-notifications (7201) Worker
        → PushProcessor
          → PushNotificationService
            → postgres-auth (5200) PushSubscription (чтение)
              → Web Push API
                → User Browser
```

### 5. Работа с Pipelines

```
User → app-web (7301)
  → api-gateway (7100)
    → api-monolith (7000)
      → postgres-monolith (5000)
        → Pipeline/Execution
```

## 📊 Маппинг портов

### API Services → Database Ports
- **Правило**: API 7xxx → DB 5xxx (изменение первой цифры: 7 → 5)

| API Service | API Port | Database Port | Database Name |
|-------------|----------|---------------|---------------|
| api-monolith | 7000 | 5000 | workix_monolith |
| api-gateway | 7100 | N/A | N/A (stateless) |
| api-auth | 7200 | 5200 | workix_auth |
| api-notifications | 7201 | 5201 | workix_notifications |

### Frontend Applications
| Application | Port | Purpose |
|-------------|------|---------|
| app-admin | 7300 | Admin Panel |
| app-web | 7301 | Web Application |

### Infrastructure
| Service | Port | Purpose |
|---------|------|---------|
| Redis | 5900 | Message Queue |

## 🔐 Безопасность

### Аутентификация
- **JWT Tokens**: Access token + Refresh token
- **2FA/TOTP**: Двухфакторная аутентификация
- **OAuth2**: Google, GitHub, Apple
- **Security Codes**: Для подозрительной активности

### Авторизация
- **RBAC**: Role-Based Access Control
- **API Keys**: Для сервис-сервис взаимодействия
- **Service Auth Guard**: Для внутренних сервисов

## 📈 Масштабируемость

### Горизонтальное масштабирование
- **api-monolith**: Можно масштабировать независимо
- **api-auth**: Можно масштабировать независимо
- **api-notifications**:
  - HTTP API можно масштабировать отдельно
  - Worker можно масштабировать отдельно
- **Redis**: Кластеризация для высокой нагрузки

### Вертикальное масштабирование
- Каждый сервис может масштабироваться независимо
- Базы данных можно масштабировать отдельно

## 🚀 Развертывание

### Development
```bash
# Запуск всех сервисов через Docker Compose
docker-compose up

# Или через NX
nx serve api-auth
nx serve api-notifications
nx serve api-monolith
nx serve api-gateway
nx serve app-admin
nx serve app-web
```

### Production
- Каждый сервис развертывается независимо
- Используется Kubernetes для оркестрации
- API Gateway для единой точки входа
- Load balancing для высокой доступности

## 📚 Документация

### Swagger UI
- **api-monolith**: `http://localhost:7000/api-monolith/docs`
- **api-auth**: `http://localhost:7200/docs`
- **api-notifications**: `http://localhost:7201/docs`
- **api-gateway**: `http://localhost:7100/docs`

### Архитектурная документация
- `apps/api-notifications/ARCHITECTURE.md` - Архитектура сервиса уведомлений
- `apps/api-notifications/ENTERPRISE_ARCHITECTURE.md` - Enterprise best practices
- `apps/api-gateway/GATEWAY_ARCHITECTURE_DIAGRAM.md` - Архитектура Gateway

## ✅ Преимущества архитектуры

1. **Разделение ответственности**: Каждый сервис отвечает за свою область
2. **Масштабируемость**: Независимое масштабирование компонентов
3. **Гибкость**: Можно запускать монолит или микросервисы
4. **Надежность**: Отказ одного сервиса не влияет на другие
5. **Простота разработки**: Единая кодовая база, переиспользование через libs
6. **Enterprise-ready**: Соответствует best practices крупных компаний
